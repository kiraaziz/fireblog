const Content =({children})=>{

    return(
        <div class="h-screen flex flex-col bg-gray-800 overflow-x-hidden overflow-y-auto">
            {children}
        </div>
    )
}

export default Content